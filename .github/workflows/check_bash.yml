name: Check bash

on: [push]


jobs:
  check_bash:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v4

      - name: Debug this
        run: ls -ltra && pwd

      - name: Check bash
        run: |
          bash -c "source .helper_bash_functions"

